all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/id_rsa
  hosts:
    server1:
      ansible_host: "{{ lookup('env', 'HOST1_IP') }}"
      vm_ip: 192.168.122.10
      wg_ip: 10.10.0.1
      node_name: node1
    # server2:
    #   ansible_host: "{{ lookup('env', 'HOST2_IP') }}"
    #   vm_ip: 192.168.122.11
    #   wg_ip: 10.10.0.2
    #   node_name: node2
  children:
    hosts:
      hosts:
        server1: {}
        # server2: {}
