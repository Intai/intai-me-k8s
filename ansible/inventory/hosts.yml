all:
  hosts:
    server1:
      ansible_host: "{{ lookup('env', 'HOST_IP') }}"
      ansible_user: ubuntu
      ansible_ssh_private_key_file: ~/.ssh/id_rsa
  children:
    hosts:
      hosts:
        server1: {}
