- name: Generate k8s-cluster.yml from hosts inventory
  ansible.builtin.template:
    src: k8s-cluster.yml.j2
    dest: "{{ playbook_dir }}/inventory/k8s-cluster.yml"
    mode: "0644"
  delegate_to: localhost
  run_once: true
  become: false
