[project]
name = "intai-me-k8s"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "ansible>=10.7.0,<11.0",
    "boto3>=1.34",
    "distlib>=0.4",
    "netaddr>=1.3",
]

[tool.setuptools]
packages = []
